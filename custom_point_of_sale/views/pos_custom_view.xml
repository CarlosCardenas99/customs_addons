<odoo>
    <data>
        <record model="ir.ui.view" id="view_pos_partner_form">
            <field name="name">pos.partner.form</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="point_of_sale.view_ClientDetailsEdit"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='name']" position="after">
                    <!-- Agrega tus campos personalizados aquí -->
                    <field name="l10n_latam_identification_type_id"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>

